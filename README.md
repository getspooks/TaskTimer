# Task Timer (C# Console App)

A simple console-based task timer written in C#.  
Start and stop named tasks, view all recorded sessions, see total time per task, export session data to CSV and clear all data.  
Sessions are saved to a local JSON file so they persist between runs.

## Features

- Start a new named task ("Study Time", "Problem 1", etc.).
- Pause the currently running task.
- Stop the currently running task.
- Resume a task previously placed on pause.
- List all "sessions" or tasks.
- Provide a summary of data per task name.
- Clear all sessions.
- Export Sessions to a CSV.

## How to Run

1. Clone the repository:
   ```bash
   git clone https://github.com/getspooks/tasktimer.git

